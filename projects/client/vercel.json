{
  "version": 2,
  "builds": [
    { "src": "build/**", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/src/index.js" },
    { "src": "/(.*)", "dest": "/build/$1" }
  ]
}